{% extends 'layout.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ team.userId }}</td>
            </tr>
            <tr>
                <th>Project_id</th>
                <td>{{ team.projectId }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ team.role }}</td>
            </tr>
           

        </tbody>
    </table>

 <h2>Project Details</h2>
    <table class="table">
        <tbody>
            <tr>
                <th>User_id</th>
                <td>{{ team.getUserId() }}</td>
            </tr>
            <tr>
                <th>Project_id</th>
                <td>{{ team.getProjectId() }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ team.getRole() }}</td>
            </tr>
            <!-- Ajoutez d'autres champs du projet selon vos besoins -->
        </tbody>
    </table>

    <a href="{{ path('app_team_index') }}">back to list</a>

    <a href="{{ path('app_team_edit', {'id': team.id}) }}">edit</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}
